version: "3.8"
services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - API_KEY=${API_KEY:-dev-key-change-me}
      - DATABASE_URL=sqlite:///./data/calls.db
    volumes:
      - ./data:/app/data
