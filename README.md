# Carrier Sales Dashboard

Real-time dashboard for inbound carrier call metrics from HappyRobot.

## Quick Start (Local)

1. Copy `.env.example` to `.env` and set your API key
2. Run with Docker:
   ```bash
   docker-compose up --build
